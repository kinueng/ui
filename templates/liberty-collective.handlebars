{
  "kind": "{{kind}}",
  "apiVersion": "{{apiVersion}}",
  "metadata": {
    "name": "{{name}}",
    "namespace": "{{namespace}}",
    {{> labels }}
  },
  "spec": {
    "console_url": "{{consoleurl}}",
    "credentials": "{{get_credentials selectCredentials secretName selectedSecret}}",
    "interval": 30
  }
}
